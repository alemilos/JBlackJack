package view.pages;

import javax.swing.*;

public class ProfilePage extends JFrame {
    public ProfilePage() {
        setTitle("Profile");
        setExtendedState(JFrame.MAXIMIZED_BOTH);
        setResizable(false);
        setVisible(true);
    }
}
